// utils/math.js

// This function adds two numbers
export function add(a, b) {
  return a + b;
}

// Intentional ESLint error: using double quotes instead of single quotes
const exampleError = "This should be single quotes";
