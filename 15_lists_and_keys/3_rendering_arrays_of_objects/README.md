# 3. Rendering Arrays of Objects

In real-world applications, you rarely render arrays of strings. Usually, you receive an array of objects from an API or database.

In these cases, you map over the objects and use one of the object's unique properties (like a database ID) as the `key` prop.
